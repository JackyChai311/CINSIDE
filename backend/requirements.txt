fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
openpyxl>=3.1
xlrd>=2.0.1
httpx>=0.27
rapidfuzz>=3.6
pydantic>=2.6
markitdown[pdf]>=0.1.0
PyMuPDF>=1.24
Pillow>=10.0
pillow-heif>=0.16  # HEIC/HEIF（iPhone 拍摄照片）解码，供文件提取 OCR

# 内置本地 OCR 引擎（「核显加速」开关）：RapidOCR PP-OCRv6 + DirectML
# （Intel/AMD/NVIDIA 通用 GPU 推理；自检不过自动锁 CPU，无需 UMI-OCR）
rapidocr>=3.9
onnxruntime-directml>=1.20
openvino>=2024.3  # CPU 推理加速候选：Intel CPU 指令级优化，AMD 亦可用（自检择优）

# 真实跑 Browser Use 时再装（依赖较重）
# browser-use>=0.1.30
# langchain-openai>=0.1.0
# 然后执行: playwright install chromium
